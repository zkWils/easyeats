[tool.poetry]
name = "easyeats"
version = "0.1.0"
description = "Food ordering and restaurant reservation platform"
authors = ["Clara chizaramclara24@gmail.com"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12,<3.15"
django = "^6.0"
gunicorn = "^23.0.0"
whitenoise = "^6.8.0"
pillow = "^11.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
